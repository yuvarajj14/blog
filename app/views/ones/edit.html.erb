<%= form_for :one, url: one_path, html: { multipart: true }, method:"patch"  do |f|%>
  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@article.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @article.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<p>
  <%= f.label :title%>
  <%= f.text_field :title%>
</p>
<p>
  <%= f.label :image%>
  <%= f.file_field :image%>
</p>
<p>
  <%= f.label :text%>
  <%= f.text_area :text%>
</p>
<p>
  <%= f.submit%>
</p>
<%= link_to "back",ones_path %>

<%end%>